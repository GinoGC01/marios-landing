---
import Layout from '../layouts/Layout.astro';
import ArrowRight from '../components/Icons/ArrowRight.astro';

const errorSchema = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Error 404 - Activo No Localizado | Mario's Agency",
  "description": "La infraestructura técnica no pudo localizar el recurso solicitado."
};
---

<Layout title="404 - Activo No Localizado">
  <script type="application/ld+json" set:html={JSON.stringify(errorSchema)} />

  <main id="main-404" class="relative flex items-center justify-center overflow-hidden z-[50]">
      <div class="absolute inset-0 pointer-events-none bg-radial-red opacity-60" aria-hidden="true"></div>
    
    
    <div class="container relative z-10 mx-auto px-6 text-center">
      
      <div class="inline-flex items-center gap-2 rounded-full bg-red-500/5 border border-red-500/20 px-5 py-2 mb-8 animate-fade-in">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-500 opacity-50"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-red-500"></span>
        </span>
        <span class="text-[10px] font-bold text-red-500 uppercase tracking-[0.3em] font-zalando">
          Error de protocolo 404
        </span>
      </div>

      <h1 class="text-4xl md:text-8xl font-black text-white mb-6 font-zalando uppercase tracking-tighter leading-none">
        Activo <span class="text-gradient">No Encontrado</span>
      </h1>

      <p class="text-gray-400 text-base md:text-xl max-w-2xl mx-auto mb-12 font-zalando leading-relaxed">
        La ruta solicitada no devolvió una respuesta válida de nuestra infraestructura. 
        Hemos reorientado los sistemas para que puedas volver al panel principal.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-6">
        <a 
          href="/" 
          class="cta-primary group relative px-10 py-5 bg-gradient-to-r from-[#0099ff] to-[#a855f7] text-white font-bold rounded-full overflow-hidden transition-all duration-300 hover:shadow-[0_0_40px_rgba(0,153,255,0.6)] hover:scale-105"
        >
          <div class="absolute inset-0 bg-gradient-to-r from-[#a855f7] to-[#0099ff] opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          <span class="relative z-10 flex items-center gap-2 uppercase text-sm tracking-widest">
            Restablecer Conexión
            <ArrowRight />
          </span>
        </a>

        <a 
          href="/contact" 
          class="text-gray-500 hover:text-green-400 transition-all duration-300 font-bold uppercase text-[10px] tracking-[0.2em] font-zalando border-b border-transparent hover:border-green-400/30 pb-1"
        >
          Solicitar Soporte Técnico
        </a>
      </div>

      <div class="mt-20 opacity-20 hidden md:block">
        <div class="max-w-md mx-auto bg-white/5 rounded-lg p-4 font-mono text-[10px] text-left text-cyan-400">
          <p>> GET /url_solicitada HTTP/1.1</p>
          <p>> Host: mariosagency.com</p>
          <p class="text-red-400">> HTTP/1.1 404 Not Found</p>
          <p>> [SYSTEM]: Re-routing user to safe zone...</p>
        </div>
      </div>

    </div>
  </main>
</Layout>

<style>
  .text-gradient {
    background: linear-gradient(to r, #00E5FF, #a855f7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

    .bg-radial-red {
    background: radial-gradient(
      circle at center, 
      rgba(224, 0, 0, 0.1) 0%, 
      transparent 75%
    );
  }

  /* Animación de entrada fluida */
  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .cta-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.2),
      transparent
    );
    transition: 0.5s;
  }

  .cta-primary:hover::before {
    left: 100%;
  }

    main{
    height: 100vh;
  }


</style>